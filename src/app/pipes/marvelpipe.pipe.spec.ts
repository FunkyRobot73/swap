import { MarvelpipePipe } from './marvelpipe.pipe';

describe('MarvelpipePipe', () => {
  it('create an instance', () => {
    const pipe = new MarvelpipePipe();
    expect(pipe).toBeTruthy();
  });
});
